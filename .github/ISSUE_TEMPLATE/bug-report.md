---
name: Bug report or improvement
about: A defect or improvement in the template itself - not your own job search
---

<!-- Before you file: this tracker is PUBLIC.

     If you made your copy with "Use this template", `gh` already points at
     your own repository and you are fine. If you cloned this repository
     directly instead, `gh issue create` files HERE, on a public tracker -
     and the things you would naturally paste (a posting you are ranking, a
     fit evaluation, a cover-letter draft, a tracker row, a /rank or /apply
     transcript) name your institutions, your referees and where you are
     applying.

     Run `gh repo set-default <you>/<your-copy>` in your clone to point your
     own automation home. See SETUP.md, section 6.

     Reporting a bug here is welcome. Reduce it to the smallest example that
     shows the problem, with placeholder names. -->

## What happened

## Steps to reproduce

## What you expected

## Where it happened

<!-- Command (/setup, /scrape, /rank, /apply, /outcome, /interview, /reset),
     file, or tool. `python3 tools/security_guards.py` and
     `python3 -m unittest discover -s tests` output helps if either fails. -->
